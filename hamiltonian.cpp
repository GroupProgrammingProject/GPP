#include <iostream>
#include <vector>

double get_hijab(int a, int b, std::vector<double> d);

// Takes 4 vector arguments and 1 int: type, posx, posy, posz, n
int main(int argc, char **argv) { 
  int i, j, a, b; // i,j loop over atoms; a,b loop over orbitals
  for (i=0:i<n;i++) {
    for (j=0:j<i;j++) {
      int mi = 4; //int mi = tot_orbitals(type[i]); // Number of orbitals of atom i
      int mj = 4; //int mj = tot_orbitals(type[j]);
      for (a=0:a<m;a++) {
      }}}
}
