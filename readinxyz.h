// Read in from file functionality
#include <iostream>
#include <string>
#include <vector>

// Reads in 4 vectors from cell file
void ReadInXYZ(std::string filename, std::vector<std::string>* names, std::vector<double> x, std::vector<double> y, std::vector<double> z){
	ifstream infile;
	infile.open(filename);
	string textarray[10];
	for ()
}
